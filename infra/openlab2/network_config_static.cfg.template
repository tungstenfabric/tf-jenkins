version: 2
ethernets:
  ens3:
     dhcp4: false
     addresses: [ ${VM_IP_ADDRESS}/${VM_NETMASK} ]
     gateway4: ${VM_GATEWAY}
     nameservers:
       addresses: [ ${VM_NAMESERVERS} ]
       search: [ ${VM_DOMAIN_NAME} ]
