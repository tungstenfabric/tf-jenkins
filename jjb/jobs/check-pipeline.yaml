---

- job:
    name: check-pipeline
    node: aws
    description: check-pipeline
    project-type: pipeline
    pipeline-scm:
      scm:
        - tf-jenkins
      script-path: jjb/pipelines/check.pipeline
      lightweight-checkout: true
