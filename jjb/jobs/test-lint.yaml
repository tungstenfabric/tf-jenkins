- job:
    name: "test-lint"
    description: "lint"
    defaults: global
    parameters:
      - label:
          name: SLAVE
    builders:
      - shell: |
          echo "//docker pull tf-dev-env; tf-dev-env/run.sh lint"
