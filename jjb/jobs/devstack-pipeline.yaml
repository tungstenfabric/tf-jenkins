---

- job:
    name: devstack-pipeline
    node: aws
    description: devstack-pipeline
    project-type: pipeline
    pipeline-scm:
      scm:
        - tf-jenkins
      script-path: src/progmaticlab/tf-jenkins/jjb/pipelines/devstack.pipeline
      lightweight-checkout: true